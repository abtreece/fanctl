name: runner-smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: report host
        run: |
          echo "runner: $RUNNER_NAME"
          echo "host:   $(hostname)"
          uname -a
